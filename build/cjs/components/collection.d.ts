import * as React from 'react';
import * as types from '../types';
declare const Collection: React.FC<{
    block: types.CollectionViewBlock | types.CollectionViewPageBlock;
}>;
export default Collection;
